# AutoDL 推理 worker 依赖。
#
# worker.py 复用主项目 robot_dh，最稳妥的方式是直接安装主项目：
#   pip install -e /path/to/robot-data-harness
#
# 若只想装 worker 运行所需子集（不装全主项目），至少需要下列与主项目对齐的版本：
psycopg[binary]>=3.2,<4
boto3==1.34.106
botocore==1.34.106
pyarrow>=15,<22
SQLAlchemy>=2.0,<3
s3fs==2024.6.0

# 仅当本机真正跑 GPU 推理时再装（无 GPU 环境可跳过）：
# vllm>=0.5
